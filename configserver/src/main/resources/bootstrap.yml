server:
   port: 9190
spring:
  cloud:
     config:
       server:
           native:
             search-locations: classpath:config/

  application:
    name: configserver
  profiles:
    active: native
